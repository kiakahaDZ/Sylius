{# Delegates to SyliusBannerPlugin (dynamic slides + fallback to this theme’s static hero inside plugin template). #}
{{ render(controller('Plugin\\SyliusBannerPlugin\\Controller\\Shop\\BannerController::heroAction', { position: 'hero' })) }}
